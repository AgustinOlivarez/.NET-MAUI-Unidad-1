<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:MauiApp1.ViewModels"
             xmlns:models="clr-namespace:MauiApp1.Models"
             x:Class="MauiApp1.Views.PlayersPage"
             Title="Jugadores">

    <Grid RowDefinitions="Auto, Auto, *">
        <!-- Etiqueta de Estado -->
        <Label Grid.Row="0"
               Text="{Binding StatusMessage}"
               TextColor="Red"
               HorizontalOptions="Center" 
               Margin="10" />

        <!-- Botón de Carga -->
        <Button Grid.Row="1"
                Text="Cargar Jugadores"
                Command="{Binding LoadPlayersCommand}"
                Margin="10"
                HorizontalOptions="Center" />

        <!-- Lista de Jugadores -->
        <RefreshView Grid.Row="2" IsRefreshing="{Binding IsBusy}" Command="{Binding LoadPlayersCommand}">
            <CollectionView ItemsSource="{Binding Players}" SelectionMode="Single" SelectionChangedCommand="{Binding SelectPlayerCommand}" SelectionChangedCommandParameter="{Binding SelectedItem, Source={RelativeSource Self}}">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:Player">
                        <Frame Margin="10,5" Padding="15" CornerRadius="10">
                            <StackLayout>
                                <Label Text="{Binding Nombre}" FontSize="18" FontAttributes="Bold" />
                            </StackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </RefreshView>
    </Grid>

</ContentPage>
